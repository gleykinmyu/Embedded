; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:genericSTM32F407VGT6]
platform = ststm32
board = genericSTM32F407VGT6
framework = stm32cube
upload_protocol = stlink
debug_tool = stlink

; irq.cpp в lib/board попадает в libboard.a: без этого линкер не извлекает объект,
; остаются weak USARTx_IRQHandler из startup → Default_Handler при первом IRQ.
lib_archive_flags =
    board=whole-archive

build_flags =
    -std=gnu++17
    -DHSE_VALUE=8000000U
    -Ilib/Interfaces
    -Ilib/debug
    -Ilib/FatFs

monitor_filters = send_on_enter 
monitor_echo = yes

; Два разных main в проекте — оставляем один (демо в main2.cpp).
build_src_filter = +<*> -<main2.cpp>